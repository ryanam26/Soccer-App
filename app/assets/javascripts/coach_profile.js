$(function(){
  // $(var populate_tests = function(category_id) {
        // if (category_id == test){
            // $("#category_id").show();
        // } else {
            // $("#rationcard_no").hide();
        // }
    // }

	$("#category").change(function(){
		$(this).val().show();
	    // populate_tests($(this).val())
	})
)});