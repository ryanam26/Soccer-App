- model_class = Location
div class="page-header"
  h1=t '.title', :default => model_class.model_name.human.titleize

p
  strong= model_class.human_attribute_name(:name) + ':'
  br
  = @location.name

= link_to t('.back', :default => t("helpers.links.back")), locations_path, :class => 'btn btn-default'
'
= link_to t('.edit', :default => t("helpers.links.edit")), edit_location_path(@location), :class => 'btn btn-default'
'
= link_to t('.destroy', :default => t("helpers.links.destroy")), location_path(@location), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
