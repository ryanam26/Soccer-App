h1 = @account.name
hr

.col-md-6
  .panel.panel-default
    .panel-heading Account Teams
    ul.list-group
      - @account.teams.each do |team|
        li.list-group-item 
          span.badge = "Scores = #{team.users.joins(:scores).count}"
          = team.name

.col-md-6
  .panel.panel-default
    .panel-heading Manager Users
    ul.list-group
      - @account.users.each do |user|
        li.list-group-item 
          span.badge = "Last login = #{user.last_sign_in_at.strftime("%A %B %Y at %H:%M")}"
          = "#{user.full_name} - Coach manager"
